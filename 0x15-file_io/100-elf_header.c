#include "main.h"

/**
 * is_elf - checks if a file is an ELF file or not
 * @str: a pointer of string
 */
void is_elf(unsigned char *str)
{
	int i;

	for (i = 0; i < 4; i++)
	{
		if (str[i] !=x 
	}
}
